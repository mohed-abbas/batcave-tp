# Copiez ce fichier en .env et remplissez les valeurs.
PORT=3000
# Clé de signature des JWT (ex: node -e "console.log(require(\047crypto\047).randomBytes(32).toString(\047hex\047))")
JWT_SECRET=changez_moi_par_une_cle_aleatoire

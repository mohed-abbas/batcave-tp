# Copiez ce fichier en .env et remplissez les valeurs.
PORT=3000
# Clé aléatoire pour signer les sessions (ex: node -e "console.log(require(\047crypto\047).randomBytes(48).toString(\047hex\047))")
SESSION_SECRET=changez_moi_par_une_cle_aleatoire
